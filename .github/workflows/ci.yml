name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Validate JSON and Structure
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate JSON files
        run: |
          echo "Validating data/deals.json..."
          jq empty data/deals.json && echo "✓ data/deals.json is valid JSON"
          echo "All JSON files are valid!"

      - name: Check file structure
        run: |
          echo "Checking required files..."
          test -f README.md && echo "✓ README.md exists"
          test -f CONTRIBUTING.md && echo "✓ CONTRIBUTING.md exists"
          test -f LICENSE && echo "✓ LICENSE exists"
          test -f index.html && echo "✓ index.html exists"
          test -f CNAME && echo "✓ CNAME exists"
          test -d css && echo "✓ css/ directory exists"
          test -d js && echo "✓ js/ directory exists"
          test -d data && echo "✓ data/ directory exists"
          echo "File structure is valid!"
